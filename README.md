# EthToolGUI
A GUI interface to the Linux `ethtool` command

```bash
git clone https://github.com/jay-to-the-dee/EthToolGUI
cd EthToolGUI

ant compile
ant jar
ant run

# Or shorter with
# ant compile jar run
```
